Release notes — 2026-05-01
===========================

Summary
-------
- Fixed multiple TypeScript `implicit any` errors in `onChange` handlers across `apps/web` admin and helper pages.
- Repaired `packages/ui` exports and `sidebar` component (added mobile helper and fixed Tailwind classes).
- Switched `apps/web/src/db/schema.ts` to re-export from the stable `@repo/db/schema` barrel to avoid Turbopack export issues.
- Added `VIDEO_KYC_*` keys to `turbo.json` earlier in the sweep and added `styles.d.ts` to satisfy CSS imports.

Commits included
----------------
- 27f1e89  fix(ui): restore barrel exports; add useIsMobile and tsconfig rootDir
- 1d67d74  fix(web): type onChange handlers and export stable DB schema

Notes
-----
- Builds and type-checks were validated locally: `pnpm --filter web build` completed successfully and branch pushed to `origin/main`.
- Recommended next step: create a PR for review and merge; after merge, deploy via your normal Vercel pipeline.
